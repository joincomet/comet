query blockedUsers {
  blockedUsers {
    id
    username
    commentCount
    postCount
    upvoteCount
    createdAt
    lastLogin
    isBlocking
    isBlocked
    isCurrentUser
    profile {
      avatar
    }
  }
}
