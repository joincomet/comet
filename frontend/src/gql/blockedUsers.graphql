query blockedUsers {
  blockedUsers {
    id
    username
    commentCount
    postCount
    endorsementCount
    createdAt
    lastLogin
    isBlocking
    isBlocked
    isCurrentUser
    tag
    tagColor
    avatarImageUrl
    bio
  }
}
