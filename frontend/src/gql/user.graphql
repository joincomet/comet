query user($username: String!) {
  user(username: $username) {
    id
    username
    commentCount
    postCount
    rocketCount
    createdAt
    lastLogin
    isBlocking
    isBlocked
    isCurrentUser
    timeSinceCreated
    followerCount
    followingCount
    profile {
      avatar
      bio
      realName
      website
    }
    moderatedPlanets {
      name
      profile {
        avatar
      }
    }
  }
}
