query currentUser {
  currentUser {
    id
    username
    tag
    tagColor
    profilePicUrl
    admin
    appearOffline
    moderatedPlanets {
      name
      avatarImageUrl
      themeColor
      postCount
    }
  }
}
