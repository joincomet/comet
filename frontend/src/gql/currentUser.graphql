query currentUser {
  currentUser {
    id
    username
    tag
    tagColor
    avatarImageUrl
    admin
    appearOffline
    moderatedPlanets {
      name
      avatarImageUrl
      themeColor
      postCount
    }
  }
}
