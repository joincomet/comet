mutation submitPost(
  $title: String!
  $type: PostType!
  $link: String
  $textContent: String
  $planet: String!,
  $image: Upload
) {
  submitPost(
    title: $title
    type: $type
    link: $link
    textContent: $textContent
    planet: $planet,
    image: $image
  ) {
    id
  }
}
