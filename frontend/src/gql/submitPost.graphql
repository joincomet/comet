mutation submitPost(
  $title: String!
  $link: String
  $textContent: String
  $community: String!,
  $image: Upload
) {
  submitPost(
    title: $title
    link: $link
    textContent: $textContent
    community: $community,
    image: $image
  ) {
    id
  }
}
