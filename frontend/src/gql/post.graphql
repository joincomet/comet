query post($postId: ID!) {
    post(postId: $postId) {
      author {
        id
        username
        isCurrentUser
        profile {
          avatar
        }
      }
      commentCount
      relativeUrl
      createdAt
      editedAt
      timeSince
      editedTimeSince
      upvoteCount
      id
      upvoted
      link
      textContent
      title
      sticky
      community {
        name
        userCount
        createdAt
        joined
        profile {
          avatar
        }
      }
    }
  }
