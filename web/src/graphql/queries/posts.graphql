query posts(
  $sort: PostsSort
  $page: NonNegativeInt
  $pageSize: PositiveInt
  $time: PostsTime
  $folderId: ID
  $serverId: ID
  $search: String
) {
  posts(
    sort: $sort
    time: $time
    folderId: $folderId
    serverId: $serverId
    search: $search
    page: $page
    pageSize: $pageSize
  ) {
    hasMore
    posts {
      ...Post
      author {
        ...ServerUser
      }
      server {
        ...Server
      }
    }
  }
}
