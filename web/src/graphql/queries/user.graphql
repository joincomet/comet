query user($id: ID) @live {
  user(id: $id) {
    ...RelatedUser
    folders {
      ...Folder
    }
    relatedUsers {
      ...User
    }
    servers {
      nickname
      id
      avatarUrl
      name
      initials
    }
  }
}
