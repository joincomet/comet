query user($id: ID) @live {
  user(id: $id) {
    ...RelatedUser
    folders {
      ...Folder
    }
    relatedUsers {
      ...User
    }
    servers {
      id
      avatarUrl
      name
    }
  }
}
