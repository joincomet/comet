query user($id: ID) @live {
  user(id: $id) {
    ...User
    folders {
      ...Folder
    }
    relatedUsers {
      ...User
    }
    servers {
      nickname
      id
      avatarUrl
      name
      initials
    }
  }
}
