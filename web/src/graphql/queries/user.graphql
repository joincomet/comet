query user($id: ID, $username: String) @live {
  user(id: $id, username: $username) {
    ...RelatedUser
    relatedUsers {
      ...User
    }
    servers {
      id
      avatarUrl
      name
    }
  }
}
