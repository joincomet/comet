fragment Server on Server {
  id
  name
  displayName
  description
  avatarUrl
  bannerUrl
  category
  userCount
  isJoined
  isDownvotesEnabled
  owner {
    id
  }
  permissions
  channels {
    ...Channel
  }
}
