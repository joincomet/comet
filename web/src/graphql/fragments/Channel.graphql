fragment Channel on Channel {
  id
  name
  description
  unreadCount
  mentionCount
  permissions
  rolePermissions {
    allowedPermissions
    deniedPermissions
    role {
      ...Role
    }
  }
}
