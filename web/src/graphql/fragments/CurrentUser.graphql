fragment CurrentUser on User {
  ...User
  isAdmin
  email
  servers {
    ...Server
  }
  relatedUsers {
    ...User
  }
  groups {
    ...Group
    owner {
      id
    }
    users {
      ...User
    }
  }
}
