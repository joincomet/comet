fragment CurrentUser on User {
  ...User
  isAdmin
  email
  servers {
    ...Server
  }
  relatedUsers {
    ...RelatedUser
  }
  groups {
    ...Group
    owner {
      id
    }
    users {
      ...User
    }
  }
}
