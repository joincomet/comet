name: cometx-landing
static_sites:
- name: landing
  environment_slug: html
  github:
    repo: cometx-io/cometx
    branch: main
    deploy_on_push: true
  build_command: yarn build
  source_dir: landing
  output_dir: landing/out
