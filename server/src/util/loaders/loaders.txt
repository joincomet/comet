user.servers (ServerUser[]) (mutual servers for others)
user.folders (Folder[]) (friend/public folders for others)
user.relationships (Relationship[]) (mutual friends for others)
user.groups (handle unreadCount, empty for others)

server.permissions (ServerPermission[])
channel.permissions (ChannelPermission[])

server.channels (Channel[]) (handle isUnread, mentionCount)

post.isVoted
comment.isVoted
